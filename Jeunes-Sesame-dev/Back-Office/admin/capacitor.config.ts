import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.adminjeuness.app',
  appName: 'Admin Jeunes-S',
  webDir: 'dist/admin',
  bundledWebRuntime: false
};

export default config;
