import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iteams.jeuness',
  appName: 'Jeunes-S',
  webDir: 'dist/etudiants',
  bundledWebRuntime: false
};

export default config;
